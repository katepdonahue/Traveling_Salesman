= form_for :waypoint, :url => trip_waypoint_path(@waypoint) do |f|
  = f.label :name, "Name:"
  = f.text_field :name
  = f.label :name, "Latitude:"
  = f.text_field :name
  = f.label :name, "Longitude:"
  = f.text_field :name
  %br
  = f.submit
